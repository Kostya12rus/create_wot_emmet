# uncompyle6 version 3.9.0
# Python bytecode version base 2.7 (62211)
# Decompiled from: Python 3.10.0 (tags/v3.10.0:b494f59, Oct  4 2021, 19:00:18) [MSC v.1929 64 bit (AMD64)]
# Embedded file name: scripts/common/site-packages/future-0.18.2/past/builtins/__init__.py
from future.utils import PY3
from past.builtins.noniterators import filter, map, range, reduce, zip
if PY3:
    from past.types import basestring, olddict as dict, oldstr as str, long, unicode
else:
    from __builtin__ import basestring, dict, str, long, unicode
from past.builtins.misc import apply, chr, cmp, execfile, intern, oct, raw_input, reload, unichr, unicode, xrange
from past import utils
if utils.PY3:
    __all__ = [
     'filter', 'map', 'range', 'reduce', 'zip', 
     'basestring', 
     'dict', 'str', 'long', 'unicode', 
     'apply', 'chr', 'cmp', 
     'execfile', 'intern', 'raw_input', 
     'reload', 'unichr', 
     'xrange']
else:
    __all__ = []